- name: Deploy Windows Servers
  hosts: localhost
  become: false
  gather_facts: false
  connection: local
  vars:
    ansible_port: 5986
  vars_files:
    - node-config/nodes-vmware.yml

  roles:
  - ansible-role-vmware
  
